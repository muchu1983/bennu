gitHub Url
git@github.com:muchu1983/united.git

開發使用的 python 版本
3.3.5

使用 pip-win 安裝的第三方庫
Pillow
PyInstaller

免費的 SQLite3 Browser
SQLiteStudio

Notepad++ 執行設定值
CTRL + F5
cmd /k cd "$(CURRENT_DIRECTORY)" & c:\python33\python "$(FULL_CURRENT_PATH)" & ECHO & PAUSE & EXIT

PyInstaller
開啟 pip-win執行
venv -c -i  pyi-env-name
再啟動shell
venv pyi-env-name
shell 移動到United_code目錄下
pyinstaller -F -w -i icon.ico united.py